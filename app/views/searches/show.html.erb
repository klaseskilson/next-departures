<% unless @results&.any? %>
  <p>Nothing found! Search again!</p>
  <%= render 'form' %>
<% end %>

<ul>
  <% @results&.each do |station| %>
    <li><%= link_to station[:name], show_departures_path(station[:site_id]) %></li>
  <% end %>
</ul>
